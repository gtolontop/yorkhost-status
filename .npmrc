# Disable audit warnings for known issues
audit-level=moderate

# Use legacy peer deps to avoid conflicts
legacy-peer-deps=true

# Disable optional dependencies
optional=false

# Set registry
registry=https://registry.npmjs.org/